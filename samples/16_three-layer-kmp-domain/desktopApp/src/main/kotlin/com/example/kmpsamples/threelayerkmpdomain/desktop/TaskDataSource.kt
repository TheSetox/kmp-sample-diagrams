package com.example.kmpsamples.threelayerkmpdomain.desktop

class TaskDataSource {
    fun loadTask(platform: String): String =
        "Desktop TaskDataSource loaded deterministic Details for $platform."
}
