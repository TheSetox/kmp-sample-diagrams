# iOS App

SwiftUI source for the `KMP Compose UI` sample.

Build the KMP framework or frameworks before wiring the files into an Xcode app target:

```sh
./gradlew -p samples/03_kmp-compose-ui :shared:linkDebugFrameworkIosSimulatorArm64
```

Swift files import: Shared.
