# iOS App

Open `iosApp.xcodeproj` in Xcode and run the shared `iosApp` scheme on an iOS simulator.

This is a real SwiftUI application target for the `Normal Native` sample. The target compiles every Swift file under `Sources/` and does not link a KMP framework.

No extra setup is required beyond selecting an iOS simulator.
