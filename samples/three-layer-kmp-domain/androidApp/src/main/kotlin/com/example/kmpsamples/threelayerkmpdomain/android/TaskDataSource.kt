package com.example.kmpsamples.threelayerkmpdomain.android

class TaskDataSource {
    fun loadTask(platform: String): String =
        "Android TaskDataSource loaded deterministic Details for $platform."
}
