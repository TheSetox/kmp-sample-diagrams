package com.example.kmpsamples.kmppresentationlayer.android

class TaskDataSource {
    fun loadTask(platform: String): String = "Android native data source implemented shared presentation contract for $platform."
}
