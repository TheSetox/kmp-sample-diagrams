package com.example.kmpsamples.kmppresentationlayer.desktop

class TaskDataSource {
    fun loadTask(platform: String): String = "Desktop native data source implemented shared presentation contract for $platform."
}
