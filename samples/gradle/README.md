# Shared Sample Build Metadata

The version catalog in this folder is shared by all sample projects. Each sample is still a standalone Gradle build and references this file from its own `settings.gradle.kts`.

The samples intentionally duplicate a small amount of settings boilerplate so each folder can be opened independently in an IDE.
